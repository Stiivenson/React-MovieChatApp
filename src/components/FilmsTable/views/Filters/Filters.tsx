import * as React from 'react';

import './Filters.scss';

interface IFiltersProps {
}

const Filters: React.VFC<IFiltersProps> = ({}) => {
    return (
        <div className='Filters'>
        </div>
    )
}

export default Filters;